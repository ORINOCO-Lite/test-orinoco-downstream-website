[workspace]
name = "orinoco-metadata-review"
channels = ["conda-forge"]
platforms = [{ platform = "osx-arm64", macos = "14.0" }, "linux-64"]
requires-pixi = ">=0.73,<0.77"

[dependencies]
python = ">=3.12,<3.13"
pyyaml = "==6.0.2"
git = ">=2.55,<3"

[pypi-dependencies]
datalad = ">=1.6.2,<2"

[target.linux-64.dependencies]
git-annex = "==10.20260601"

[target.osx-arm64-macos-14-0.pypi-dependencies]
git-annex = "==10.20260717"

[tasks]
review = "python tools/review.py review"
refresh-evidence = "python tools/review.py refresh-evidence"
extract-dump-research-info = { cmd = "python integrations/dump-research-info/metadata_adapter.py --downstream .", cwd = "../.." }
datalad-run-dump-research-info = { cmd = "python integrations/dump-research-info/run_with_datalad.py", cwd = "../.." }
test = { cmd = "python -m unittest discover -s ../../tests/integrations -p 'test_*.py' -v", env = { PYTHONPATH = "../.." } }
